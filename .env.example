# Keep local, Render and Railway validation builds closed to search crawlers.
SITE_INDEXABLE=false

# Optional outside production. The owned production origin is shown below.
# SITE_URL=https://bqrincon.com
